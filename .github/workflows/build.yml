name: Build SmartClip

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up MinGW
      uses: msys2/setup-msys2@v2
      with:
        msystem: MINGW64
        update: true
        install: mingw-w64-x86_64-gcc mingw-w64-x86_64-make
    
    - name: Build SmartClip
      shell: msys2 {0}
      run: |
        mingw32-make
    
    - name: Build Debug Version
      shell: msys2 {0}
      run: |
        mingw32-make debug
    
    - name: Upload Release Build
      uses: actions/upload-artifact@v4
      with:
        name: SmartClip-Release
        path: SmartClip_Native.exe
    
    - name: Upload Debug Build
      uses: actions/upload-artifact@v4
      with:
        name: SmartClip-Debug
        path: SmartClip_Native.exe
name: Build SmartClip

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up MinGW
      uses: msys2/setup-msys2@v2
      with:
        msystem: MINGW64
        update: true
        install: mingw-w64-x86_64-gcc mingw-w64-x86_64-make
    
    - name: Build SmartClip
      shell: msys2 {0}
      run: |
        mingw32-make
    
    - name: Build Debug Version
      shell: msys2 {0}
      run: |
        mingw32-make debug
    
    - name: Upload Release Build
      uses: actions/upload-artifact@v3
      with:
        name: SmartClip-Release
        path: SmartClip_Native.exe
    
    - name: Upload Debug Build
      uses: actions/upload-artifact@v3
      with:
        name: SmartClip-Debug
        path: SmartClip_Native.exe